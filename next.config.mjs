/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    localPatterns: [
      {
        pathname: "/logo-ma-petite-compta.png",
        search: "?v=20260604"
      }
    ]
  }
};

export default nextConfig;
